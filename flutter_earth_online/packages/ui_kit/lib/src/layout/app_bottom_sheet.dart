import 'package:flutter/material.dart';

/// App Bottom Sheet - 底部弹出组件
class AppBottomSheet extends StatelessWidget {
  const AppBottomSheet({
    super.key,
    required this.child,
  });

  final Widget child;

  @override
  Widget build(BuildContext context) {
    // TODO: Implement bottom sheet
    return child;
  }
}
