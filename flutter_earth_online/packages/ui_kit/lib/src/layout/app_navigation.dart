import 'package:flutter/material.dart';

/// App Navigation - 导航组件
class AppNavigation extends StatelessWidget {
  const AppNavigation({
    super.key,
    required this.child,
  });

  final Widget child;

  @override
  Widget build(BuildContext context) {
    // TODO: Implement navigation widget
    return child;
  }
}
